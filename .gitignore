# Vercel
.vercel

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Node (only relevant if you run `npm run dev` locally via npx serve)
node_modules/
npm-debug.log*

# Local env safety net (this project does not use a .env file today -
# js/config.js holds the public Supabase URL/anon key and IS committed on purpose)
.env
.env.local
